import { useContext } from "react";

const Footer = (props) => {
    return (
        <div className={`text-center m-3 text-primary`}>
           * Dragan bar *
        </div>
        );
    }

export default Footer;
