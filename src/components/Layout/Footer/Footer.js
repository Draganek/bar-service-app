import { useContext } from "react";

const Footer = (props) => {
    return (
        <div className={`text-center text-dark mt-3`}>
            * Dragan bar *
        </div>
    );
}

export default Footer;
